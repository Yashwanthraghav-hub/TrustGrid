"use client";
import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,CartesianGrid,Legend} from "recharts";
export default function AllocationChart({rows,unit="units"}:{rows:{area:string;quantity:number;shortage:number}[];unit?:string}){return <div className="chart-container" aria-label="Allocation and remaining shortage; exact values are in the table above"><ResponsiveContainer width="100%" height="100%"><BarChart data={rows} barSize={25} margin={{top:10,right:20,left:-15,bottom:0}}><CartesianGrid vertical={false} stroke="var(--border)"/><XAxis dataKey="area" tick={{fontSize:11,fill:"var(--muted)"}} axisLine={false} tickLine={false}/><YAxis tick={{fontSize:11,fill:"var(--muted)"}} axisLine={false} tickLine={false}/><Tooltip contentStyle={{background:"var(--surface)",border:"1px solid var(--border)",borderRadius:8,fontSize:12}}/><Legend wrapperStyle={{fontSize:11}}/><Bar dataKey="quantity" name={`Proposed ${unit}`} fill="#0f766e" radius={[4,4,0,0]}/><Bar dataKey="shortage" name="Remaining shortage" fill="#cbd8df" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div>}
