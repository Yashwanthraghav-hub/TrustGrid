import Link from "next/link";
import {PublicHeader} from "@/components/layout/public";
import {Notice} from "@/components/ui/core";
export default async function AuthError({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const params=await searchParams;const configured=params.code!=="not_configured";return <><PublicHeader/><main id="main" className="auth-wrap"><section className="panel auth-card"><h1>Sign-in needs another step.</h1><Notice tone="error">{configured?"Your sign-in could not be completed. Consent may have been cancelled, the session may have expired, or the provider may restrict this account.":"Google sign-in is not configured for this installation yet. An organization administrator needs to connect Supabase and Google OAuth."}</Notice><p style={{marginTop:20}}>No report was submitted and no operational access was granted. Retry sign-in, or use the public demo while account access is configured.</p><Link href="/login" className="button primary">Try sign-in again</Link><Link className="text-link" href="/demo">Open the fictional demo</Link></section></main></>}
